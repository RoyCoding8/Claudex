[project]
name = "cx-launcher"
version = "0.4.0"
description = "Claude Code launcher for CLIProxyAPI with an in-house pool router"
requires-python = ">=3.11,<3.14"
dependencies = [
    "prompt-toolkit>=3.0.51,<4",
    "python-dotenv>=1.0,<2",
]
